"""전역 예외(Exception) 클래스 정의 모듈.

이 모듈은 애플리케이션 전체(Infrastructure, Service, UI)에서 공통으로 
사용되는 커스텀 예외 클래스들을 정의합니다. 아키텍처의 가장 안쪽(Core)에 
위치하여 의존성 역전 원칙(DIP)을 완벽하게 준수합니다.
"""

class GeminiAPIError(Exception):
    """LLM API 통신 중 발생한 에러를 캡슐화하는 커스텀 예외 클래스.

    단일 책임 원칙(SRP)에 따라 API 호출 중 발생하는 다양한 하위 레벨 오류(HTTP 오류, 
    네트워크 연결 끊김, 타임아웃 등)를 애플리케이션 내부의 통일된 예외 타입으로 
    변환 및 정의하는 책임을 가집니다. 
    """
    pass
